spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/nova_v1_2
    username: root
    password: root

    
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  application:
     name: CONSUMER-SERVICE
     
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/     
     
    
     
server:
  port: 8083